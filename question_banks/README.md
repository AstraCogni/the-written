# Question Banks

Drop your TSV question banks here. All `.tsv` files in this folder are excluded from git — they stay on your machine only.

The `sample_questions.tsv` in the project root is the only question bank that ships publicly.
